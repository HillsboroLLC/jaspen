// Calculator version — bump when the formulas or scenario factors change.
// Kept separate so both the engine and the saved-estimate model can import it
// without a circular dependency.
export const CALC_VERSION = '1.0.0';
